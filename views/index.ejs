<!DOCTYPE html>
<html ng-app="app">
  <head>
    <title>Piove domani a Cave?</title>
    <style>
      html, body {
        margin:0px;
        padding:0px;
        font-family: arialblack, 'Arial Black', 'Arial Bold', Gadget, sans-serif;
      }
      body {
        background-color:white;
        text-align:center;
        overflow:hidden;
      }

      body.sole{
        background-image: url("images/sole.png");
      }

      body.pioggia{
       background-image: url("images/pioggia.png"); 
      }

      h1 {
        text-align:center;
        font-size:50vw;
        overflow:hidden;
        color: #fe504f;
        margin: 0; padding: 0;
      }
    </style>
  </head>
  <body ng-controller="AppCtrl" class="{{icon}}">
    <h1>{{ forecast }}</h1>

    <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.2.16/angular.min.js"></script>
    <script src="javascripts/app.js"></script>
  </body>
</html>
